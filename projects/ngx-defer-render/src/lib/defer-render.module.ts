import { NgModule } from '@angular/core';
import { DeferRenderDirective } from './defer-render.directive';

@NgModule({
  imports: [
  ],
  declarations: [DeferRenderDirective],
  exports: [DeferRenderDirective]
})
export class DeferRenderModule { }
