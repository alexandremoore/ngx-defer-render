import { DeferRenderDirective } from './defer-render.directive';

describe('DeferRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new DeferRenderDirective();
    expect(directive).toBeTruthy();
  });
});
